import { createTheme } from '@mui/material/styles';

export const lightMode = createTheme ({
    palette: {
      primary: {
        main: '#eceff1',
      },
      secondary: {
        main: '#eceff1',
      },
      background: {
        default: '#546e7a',
      },
    },
})