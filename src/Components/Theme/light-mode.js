import { createTheme } from '@mui/material/styles';

export const lightMode = createTheme ({
    palette: {
      primary: {
        main: '#cddc39',
      },
      secondary: {
        main: '#ffeb3b',
      },
      background: {
        default: '#00695f',
      },
    },
})